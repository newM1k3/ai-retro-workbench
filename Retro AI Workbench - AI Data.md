# Retro AI Workbench - AI Data

AI DATA 
Subject: Retro AI Workbench — Default Punch Card JSON Generation

Context: Dave is going to need structured data to feed into the UI while he builds the drag-and-drop Hopper mechanics. He should not be wasting time typing out dummy text.

Specific Deliverables Required:

The Seed File: Generate a default-cards.json file containing five highly detailed, structured prompt templates.

Data Structure: Each object must include a realistic prompt title (e.g., "React Component Scaffold"), the raw prompt text, the intended AI model routing (Claude/GLM-5.2), and a simulated token-length estimation. Ensure the data perfectly matches the PocketBase schema defined by The Architect.